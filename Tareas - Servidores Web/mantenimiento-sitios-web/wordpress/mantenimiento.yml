---
- name: Mantenimiento WordPress Producción
  hosts: SRV-CPWEB01, SRV-CPWEB02
  become: yes
  gather_facts: yes

  vars:
    wp_base_path: /home
    wp_bin: /usr/local/bin/wp

    wp_sites:
      - domain: algry.com
        user: algry9302
      - domain: visitarbodegasriberadelduero.com
        user: visit5841
      - domain: visitarbodegasrioja.com
        user: visit7691
      - domain: bodegasduron.com
        user: bodeg3787
      - domain: nutribenprofesionales.es
        user: nutri8393
      - domain: nutribeninternational.com
        user: nutri7976

  pre_tasks:

    - name: Avisar inicio mantenimiento
      uri:
        url: "https://api.telegram.org/bot{{ telegram_bot_token }}/sendMessage"
        method: POST
        body_format: form-urlencoded
        body:
          chat_id: "{{ telegram_chat_id }}"
          text: |
            🚧 Inicio mantenimiento WordPress
            🖥 Servidor: {{ inventory_hostname }}
            🌐 Sitios: {{ wp_sites | map(attribute='domain') | join(', ') }}
            🕒 {{ ansible_date_time.iso8601 }}
      delegate_to: localhost
      become: false

  tasks:

    - name: Mantenimiento WordPress
      block:

        - name: Activar mantenimiento
          shell: "{{ wp_bin }} maintenance-mode activate"
          args:
            chdir: "{{ wp_base_path }}/{{ item.domain }}/public_html"
          loop: "{{ wp_sites }}"
          become_user: "{{ item.user }}"
          failed_when: false

        - name: Actualizar core
          shell: "{{ wp_bin }} core update"
          args:
            chdir: "{{ wp_base_path }}/{{ item.domain }}/public_html"
          loop: "{{ wp_sites }}"
          become_user: "{{ item.user }}"
          register: core_update
          failed_when: false

        - name: Actualizar plugins
          shell: >
            {{ wp_bin }} plugin update --all
            --exclude=advanced-custom-fields-pro,js_composer,admin-menu-editor-pro,ame-branding-add-on,wp-toolbar-editor
          args:
            chdir: "{{ wp_base_path }}/{{ item.domain }}/public_html"
          loop: "{{ wp_sites }}"
          become_user: "{{ item.user }}"
          register: plugin_update
          failed_when: false

        - name: Actualizar themes
          shell: "{{ wp_bin }} theme update --all"
          args:
            chdir: "{{ wp_base_path }}/{{ item.domain }}/public_html"
          loop: "{{ wp_sites }}"
          become_user: "{{ item.user }}"
          register: theme_update
          failed_when: false

        - name: Notificar errores detectados
          uri:
            url: "https://api.telegram.org/bot{{ telegram_bot_token }}/sendMessage"
            method: POST
            body_format: form-urlencoded
            body:
              chat_id: "{{ telegram_chat_id }}"
              text: |
                🚨 Error en mantenimiento WordPress
                🌐 Sitio: {{ item.item.domain }}
                ❌ Código retorno: {{ item.rc }}
          loop: >
            {{
              (core_update.results + plugin_update.results + theme_update.results)
              | selectattr('rc', 'ne', 0)
              | list
            }}
          when: >
            (
              (core_update.results + plugin_update.results + theme_update.results)
              | selectattr('rc', 'ne', 0)
              | list
              | length
            ) > 0
          delegate_to: localhost
          become: false

      always:

        - name: Desactivar mantenimiento (SIEMPRE)
          shell: "{{ wp_bin }} maintenance-mode deactivate"
          args:
            chdir: "{{ wp_base_path }}/{{ item.domain }}/public_html"
          loop: "{{ wp_sites }}"
          become_user: "{{ item.user }}"
          failed_when: false

  post_tasks:

    - name: Resumen final mantenimiento
      uri:
        url: "https://api.telegram.org/bot{{ telegram_bot_token }}/sendMessage"
        method: POST
        body_format: form-urlencoded
        body:
          chat_id: "{{ telegram_chat_id }}"
          text: |
            ✅ Mantenimiento WordPress finalizado
            🖥 Servidor: {{ inventory_hostname }}
            🌐 Sitios totales: {{ wp_sites | length }}
            🔄 Core OK: {{ core_update.results | selectattr('rc','eq',0) | list | length }}
            🔌 Plugins OK: {{ plugin_update.results | selectattr('rc','eq',0) | list | length }}
            🎨 Themes OK: {{ theme_update.results | selectattr('rc','eq',0) | list | length }}
            🕒 {{ ansible_date_time.iso8601 }}
      delegate_to: localhost
      become: false
