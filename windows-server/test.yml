---
- name: Probar conectividad Windows – ipconfig
  hosts: all
  gather_facts: no
  become: false

  tasks:

    - name: Ejecutar ipconfig
      win_shell: netstat -ano | findstr :10000
      register: netstat_result

    - name: Mostrar salida de netstat
      debug:
        msg: "{{ netstat_result.stdout }}"
