---
- name: Probar conectividad Windows – ipconfig
  hosts: all
  gather_facts: no
  become: false

  tasks:

    - name: Ejecutar ipconfig
      win_shell: Get-Service -Name dns
      register: service_dns_result

    - name: Mostrar salida de service
      debug:
        msg: "{{ service_dns_result.stdout }}"
